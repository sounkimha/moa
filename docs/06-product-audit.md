# 제품·UX·코드 점검 — 2026-09-15

## 범위와 소스 동기화

GitHub `origin/main`의 웹 편집 커밋 `ee94fea`를 fetch한 뒤 로컬 `main`에 fast-forward로 반영했다. 웹 편집본의 아시아 도시 사진, 거래 체험 흐름, 품절·구매 불가 처리, 개발 프록시 개선을 기존 로컬 항공권 확인·초안 보존·가격 및 매칭 수정과 함께 합쳤다. 충돌 파일은 제품 정책에 맞춰 수동 통합했으며 강제 덮어쓰기·push는 하지 않았다. 통합 전 로컬 상태는 `stash@{0}`에 안전 사본으로 남겼다.

기존 Expo/React Native/TypeScript, NestJS, 공통 도메인 구조를 유지했다. 구매자/여행자 화면, 요청·묶음·거래·계정, 상태/경로, 가격 및 수락 검증, 인증·저장소·API 오류 처리를 점검했다. 전체 서비스 출시 검증이나 모든 화면의 전수 검증을 완료했다는 의미는 아니다.

## 실제 수정 사항

| 우선순위 | 발견한 문제 | 조치 |
| --- | --- | --- |
| P0 | 이전 계정의 늦은 응답이 계정 전환·로그아웃 뒤 화면을 덮어쓸 수 있음 | 세션 세대와 최신 조회 순서 확인, 인증/변경 중 중복 실행 차단 |
| P0 | 계정 전환 시 요청 초안에 이전 주소·사진이 남음 | 초안 소유자 검증, 전환·로그아웃 시 메모리/탭 저장소 정리 |
| P1 | 새로고침 시 요청 작성 내용과 경로 부가 선택값 소실 | 웹 탭 초안 복원, 경로에 안전한 식별자·입력 방식·묶음 ID 직렬화 |
| P1 | 잘못 인코딩된 URL에서 초기 렌더 오류 | 안전한 경로 파서와 홈 복구, 알 수 없는 구매 장소 방어 |
| P1 | 재부탁 작성 중 수정 내용이 이전 상품 값으로 돌아감 | 원 요청 ID와 진입 장소별 초안 문맥 분리 |
| P1 | 링크 인식 상품이 기존 진입 장소에 고정되어 가격 통화가 엇갈릴 수 있음 | 인식한 구매 장소 적용, 복원 후 불필요한 재인식 방지 |
| P1 | 귀국 전 수령 약속이 서버 검증을 통과할 수 있음 | 수령일은 귀국 이후로 제한, 달력 최소일 반영 |
| P1 | 같은 장소의 택배·직거래 혼합 묶음이 첫 요청 전달 방식으로 실패 | 각 구매자가 고른 전달 방식을 서버에서 개별 적용 |
| P1 | 수수료·순보상 독립 반올림으로 1원 차이 발생 가능 | 거래별 공통 정산 함수 사용, 묶음도 개별 수수료 합산 |
| P1 | 귀국 국가/직거래 도시가 맞지 않거나 지난 일정의 부탁 추천 | 추천 조건에 귀국 국가·도시·종료일 반영 |
| P1 | 이미 수락한 수량을 빼지 않은 묶음 선택, 만료된 선택 ID 전달 | 남은 처리 수량·10건 제한 안내, 현재 유효한 ID만 전달 |
| P2 | 모든 카드 그림자, 작은 화면의 긴 CTA/칩, 빈 상태의 막힌 흐름 | 카드 그림자 제거·20px radius, 텍스트 줄바꿈·44px 터치 영역, 복귀/등록 버튼 |
| P2 | 달력이 이전 달에 고정되거나 잘못된 값에 취약 | 열 때 선택일/최소일로 이동, 날짜 파싱 방어, 여행 시작일 변경 시 종료일 조정 |
| P2 | 상품 이미지 실패 시 빈 이미지, API 오류 문구가 항상 ‘분석’ | 사진 실패 상태와 업무별 네트워크 오류 문구 |
| P2 | 하단 CTA를 누른 뒤 상단의 검증 오류·새 결과를 놓침 | 요청 단계/오류, 여행 등록 오류, 항공권 대조 결과 변경 시 상단으로 이동 |

배송 정책은 국제배송 없이 국내 택배(데모 3,500원)·직거래(0원)다. 여행자가 부탁별 보상을 직접 제시하며, 운영 수수료는 여행자 보상의 10%를 정산 시 공제한다. 구매자에게 플랫폼 수수료를 추가하지 않는다.

## 항공권 확인의 구현 경계

`POST /api/trips/:id/flight-proof`는 소유자 인증, 필수 개인정보 처리 동의, 이미지 제한, QR/바코드 인식, 선택 OCR, 왕복 일정 대조를 수행한다. 원본/이름/예약번호/QR 원문을 저장하지 않으며 타인의 snapshot에 대조 결과를 포함하지 않는다. 예약 링크 QR은 따라가지 않는다.

연도 없는 탑승권 코드는 연도 확인 필요로 표시한다. 일정 일치는 `PENDING_REVIEW`, 불일치는 `NEEDS_REVIEW`이며 최종 인증으로 승격하지 않는다. 새 일정은 단건·묶음 수락과 예전 제안 매칭 모두 차단한다. 실제 발권/본인확인 연동 없이는 진짜 여행 인증이나 노쇼 방지를 보장할 수 없다.

## 코드 구성과 검증

- 경로 파서/직렬화는 `state/navigation.ts`, 웹 탭 초안 검증/저장은 `state/draft-session.ts`로 분리했다. 민감한 주소·사진·토큰을 경로 URL에 넣지 않는다.
- 기존 거래 원장·멱등성·리비전·권한·파일 저장 및 PostgreSQL 테스트를 유지했다. `mobile-state.test.cjs`, `matching-logic.test.cjs`, `flight-proof.test.cjs`에 회귀 테스트를 추가했다.
- `npm run typecheck` 통과, `npm test` 54개 통과, `npm run test:ui` 및 `git diff --check` 통과. `test:ui`에는 웹 export 빌드와 실제 API에 연결한 DOM 거래 흐름이 포함된다.
- 별도 실제 Chrome 자동화에서는 320/360/390/430px 화면, 요청 초안 새로고침/뒤로가기, 등록, 합성 왕복 QR 파일 업로드, 인증 대기·수락 차단, MY 넘침, 잘못된 URL과 지연된 이전 계정 응답을 검사한다. 합성 탑승권만 쓰고 임시 API 저장소를 사용한다. 지도 타일은 테스트 응답으로 대체한다.
- 브라우저 회귀 검사는 `scripts/browser-audit.py`에 보존했다. Python 3의 Playwright·requests와 Chrome이 설치된 환경에서 API 빌드 및 `npm run dev` 후 `python3 scripts/browser-audit.py`로 재현한다. 화면 캡처는 `/tmp/moa-audit-*.png`에 생성된다. 일반 `npm test` 실행에 이 Python 환경은 필요하지 않다.
- ESLint 설정/명령은 현재 없다. 타입 검사나 `git diff --check`를 lint 통과로 간주하지 않는다. 네이티브 iOS/Android 기기·키보드·스크린리더 동작은 별도 실기기 검증이 필요하다.

## 남은 사항과 다음 우선순위

1. **실계정·여행 인증과 결제**: 본인확인/항공사 증빙 공급자, 심사·이의제기, 실제 PG/webhook/대사·환불 정책을 연결한다. 현재는 데모 인증/결제이며 운영 모드 시작이 차단된다.
2. **실제 장소·동선·재고 데이터**: 카카오 검색 키, 해외 장소 검색, 경로 공급자, 판매처별 재고 갱신을 연결한다. 근처 여행자·방문자 통계·추가 이동 시간은 예시이며 실제 GPS 기반 사람 목록/최적 경로로 오인하면 안 된다. AI 인식/한국어 번역은 키와 외부 품질 검증이 필요하고 전용 모델 학습을 수행한 상태는 아니다.
3. **운영/모바일 품질**: 작은 iPhone·Android 실기기 접근성/키보드, 네이티브 보안 초안 저장, lint/CI, 페이지네이션·이미지 저장소, 데이터 50건 이상·오프라인·장시간 네트워크 장애 테스트를 강화한다. 여행 등록 초안의 새로고침 복원은 아직 미구현이다.

추가 출시 점검: 외부 URL 수집의 DNS rebinding/네트워크 egress 방어, 이미지 악성 파일 검사, 민감정보 보관·삭제 정책, 실제 위치 공유 동의, 푸시 알림, 재고 부족/가격 변경 승인, 분쟁 운영 화면. 데모 데이터의 실인증 전환이나 예시 수치를 실제 측정값으로 바꾸는 임의 처리는 하지 않았다.
